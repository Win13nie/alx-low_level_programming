#include "main.h"
#include <stdio.h>

/**
 * main - a program that prints out its name
 * @argc: number of commands on command line
 * @argv: arrays
 * Return: 0
 */

int main( int argc, char *argv[])
{
	printf("%s\n", argv[argc *0]);

	return (0);
}
