#ifndef FUNCTION_LIKE_MACRO_H_
#define FUNCTION_LIKE_MACRO_H_

/**
 * file - function_like_macro.h
 * Desc:a header file that defines a function-like macro ABS(x) that computes the absolute value of a number x
 *
 */

#define ABS(x) (((x)<0) ? -(x) : (x))

#endif
