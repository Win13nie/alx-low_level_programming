#include "main.h"
#include <stdio.h>

/**
 * main - a program that prints it's name
 * @argc: the number of command in a line argument
 * @argv: the array
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc *0]);

	return (0);
}
